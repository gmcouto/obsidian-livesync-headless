---
phase: 03-recoverable-pull-operations
plan: 04
type: execute
wave: 4
depends_on:
  - "03-01"
  - "03-02"
  - "03-03"
files_modified:
  - tests/integration/couchdb-harness.ts
  - tests/integration/pull-recovery.test.ts
  - tests/integration/pull-apply.test.ts
autonomous: true
requirements:
  - PULL-04
  - PULL-06
  - PULL-08
  - SAFE-05
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "Testcontainers CouchDB integration test proves rerun of completed pull produces 100% noop actions, 0 vault file writes, and 0 CouchDB mutations (PULL-08)"
    - "Testcontainers CouchDB integration test proves remote deletion safely moves local vault file to external quarantine, indexes row in SQLite quarantine table, and removes vault file (PULL-04)"
    - "Testcontainers CouchDB integration test proves interrupted pull with leftover .ols-tmp-* staging files resumes cleanly and completes vault materialization (PULL-06)"
    - "All SQLite database files and quarantine folders reside strictly outside the vault directory tree (SAFE-05)"
    - "CouchDB update_seq is verified unchanged before and after every pull operation (SAFE-01 zero-mutation invariant)"
  artifacts:
    - "tests/integration/couchdb-harness.ts extended with seedDeletedNote helper"
    - "tests/integration/pull-recovery.test.ts covering idempotent rerun, deletion quarantine, crash recovery, and state isolation"
  key_links:
    - "pull-recovery.test.ts uses real CouchDB container to execute end-to-end pull workflows and asserts filesystem, SQLite, and CouchDB states"
  assumptions:
    - "Docker is available for Testcontainers CouchDB execution during integration test phase"
  prohibitions:
    - "MUST NOT issue HTTP PUT, POST, or DELETE requests against CouchDB during any pull run"
    - "MUST NOT leave temporary staging files in vault or leak secrets in error output"
---

## Phase Goal

**As a** vault operator, **I want** full end-to-end integration tests running against a real CouchDB instance, **so that** idempotent reruns, collision-safe deletion quarantine, crash recovery, and external state isolation are rigorously validated before production deployment.

<objective>
Build and verify comprehensive end-to-end CouchDB integration tests:
1. Extend `tests/integration/couchdb-harness.ts` with deletion/tombstone helpers.
2. Implement `tests/integration/pull-recovery.test.ts` to validate:
   - PULL-08: Deterministic idempotent rerun (0 writes, all `noop`, exit 0).
   - PULL-04: Remote deletion handling (file moved to quarantine, recorded in SQLite, deleted from vault).
   - PULL-06: Crash/interruption recovery (orphan staging files cleaned, remaining files materialized, checkpoint saved).
   - SAFE-05: Durable state isolation (all SQLite tables and quarantine files reside outside vault).
3. Verify entire test suite (`npm test`).

Purpose: Provide undeniable automated proof that Phase 3 requirements are satisfied under real CouchDB conditions.
Output: Real CouchDB integration test suite passing with 100% reliability.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-recoverable-pull-operations/03-RESEARCH.md
@.planning/phases/03-recoverable-pull-operations/03-PATTERNS.md
@.planning/phases/03-recoverable-pull-operations/03-VALIDATION.md
@tests/integration/couchdb-harness.ts
@tests/integration/pull-apply.test.ts
@tests/integration/pull-dry-run.test.ts
@src/cli/commands/pull.ts
</context>

<tasks>

<task type="tracer">
  <name>Extend CouchDB harness with deletion/tombstone helpers</name>
  <files>
    tests/integration/couchdb-harness.ts
  </files>
  <read_first>
    .planning/phases/03-recoverable-pull-operations/03-RESEARCH.md
    .planning/phases/03-recoverable-pull-operations/03-PATTERNS.md
    tests/integration/couchdb-harness.ts
  </read_first>
  <behavior>
    - seedDeletedNote inserts or updates a CouchDB document with deleted: true and _deleted: true
    - getDocumentRev retrieves the current _rev of a document
  </behavior>
  <action>
    In `tests/integration/couchdb-harness.ts`:
    1. Add helper `seedDeletedNote(dbName: string, relativePath: string, options?: { encrypted?: boolean; passphrase?: string })`:
       - Calculates `docId = path2id_base(relativePath, false, true)`.
       - Fetches existing doc if present to acquire `_rev`.
       - PUTs document with `_id: docId`, `_rev: existingRev`, `type: 'notes'`, `path: relativePath`, `deleted: true`, `_deleted: true`, `mtime: Date.now()`.
    2. Export helper functions for use in recovery integration tests.
  </action>
  <acceptance_criteria>
    - `tests/integration/couchdb-harness.ts` exports `seedDeletedNote`
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/smoke.test.ts</automated>
  </verify>
  <done>
    CouchDB test harness supports seeding deleted documents and tombstones for integration testing.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Implement end-to-end pull recovery integration test suite</name>
  <files>
    tests/integration/pull-recovery.test.ts
    tests/integration/pull-apply.test.ts
  </files>
  <read_first>
    .planning/phases/03-recoverable-pull-operations/03-RESEARCH.md
    .planning/phases/03-recoverable-pull-operations/03-PATTERNS.md
    tests/integration/pull-apply.test.ts
    tests/integration/couchdb-harness.ts
    src/cli/commands/pull.ts
  </read_first>
  <behavior>
    - Test 1 (PULL-08 Idempotency):
      - Seeds 3 notes in CouchDB. Runs pull -> creates 3 files, exits 0.
      - Runs pull again -> report has 3 noops, 0 creates, 0 deletes, exits 0, update_seq untouched.
    - Test 2 (PULL-04 Deletion Quarantine):
      - Seeds note in CouchDB, runs pull -> file exists in vault.
      - Seeds deletion of the note in CouchDB, runs pull -> file removed from vault, file present in <stateRoot>/quarantine/, row in SQLite quarantine table, file_provenance deleted, CouchDB update_seq untouched.
    - Test 3 (PULL-06 Crash / Interruption Resumption):
      - Seeds 2 notes in CouchDB.
      - Writes a stale orphan staging file '.ols-tmp-interrupted' into vault.
      - Runs pull -> preflight cleans orphan, pulls both notes, checkpoint is saved in SQLite, exits 0.
    - Test 4 (SAFE-05 Durable State Isolation):
      - Inspects vault directory: contains ONLY user notes, zero SQLite files, zero quarantine folders.
      - Inspects state directory: contains livesync.db (with tables remote_admission, file_provenance, quarantine, pull_checkpoints) and quarantine/ directory.
  </behavior>
  <action>
    Create `tests/integration/pull-recovery.test.ts`:
    - Setup Testcontainers CouchDB container with standard lifecycle (beforeAll/afterAll).
    - Implement the 4 test scenarios above using temporary directories for `vault` and `state`.
    - Run `runPullCommand` with injectable `stdout` and check return code, JSON report, filesystem state, SQLite state, and CouchDB `_changes` / `update_seq`.
  </action>
  <acceptance_criteria>
    - `tests/integration/pull-recovery.test.ts` passes all 4 end-to-end scenarios against Testcontainers CouchDB
    - `npx vitest run tests/integration/pull-recovery.test.ts` passes
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/integration/pull-recovery.test.ts</automated>
  </verify>
  <done>
    Real CouchDB integration tests prove PULL-04, PULL-06, PULL-08, and SAFE-05 requirements are fully satisfied.
  </done>
</task>

<task type="auto">
  <name>Full project test suite validation</name>
  <files>
    tests/integration/pull-recovery.test.ts
    tests/integration/pull-apply.test.ts
    tests/integration/pull-dry-run.test.ts
    tests/integration/inspect-command.test.ts
  </files>
  <read_first>
    tests/integration/pull-recovery.test.ts
    tests/integration/pull-apply.test.ts
  </read_first>
  <action>
    Run the complete test suite across unit, characterization, and integration tests to guarantee zero regressions.
  </action>
  <acceptance_criteria>
    - `npm test` passes with 100% success across all unit and integration test files
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    Full project test suite is completely green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CouchDB Remote | Network transport over HTTP/HTTPS with basic authentication and CouchDB protocol payloads |
| Vault / State Isolation | Strict physical separation between vault workspace and external metadata state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-11 | Tampering / Remote Mutation | ZeroMutationVerifier / CouchDB transport | high | mitigate | Integration test asserts CouchDB `update_seq` before and after pull across all scenarios, proving 0 remote mutations |
| T-03-12 | Data Loss | quarantineVaultFile | high | mitigate | Integration test verifies deleted note is preserved in quarantine directory with intact bytes before vault unlink |
| T-03-13 | State / Vault Pollution | vault-preflight / sqlite | medium | mitigate | Integration test asserts zero `.sqlite*` or `quarantine/` entries ever enter the vault directory |
</threat_model>

<verification>
npx vitest run tests/integration/pull-recovery.test.ts
</verification>

<success_criteria>
- All integration tests in pull-recovery.test.ts pass against real CouchDB container
- Full npm test passes with zero failures
- PULL-04, PULL-06, PULL-08, and SAFE-05 requirements verified by integration tests
</success_criteria>

<output>
Create `.planning/phases/03-recoverable-pull-operations/03-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- Test helpers: `seedDeletedNote` (`tests/integration/couchdb-harness.ts`)
- Integration tests: `tests/integration/pull-recovery.test.ts`
