---
phase: 06-packaged-mixed-client-release-gate
plan: 04
type: execute
wave: 4
depends_on:
  - 06-01
  - 06-02
  - 06-03
files_modified:
  - scripts/audit-release.ts
  - tests/unit/release-audit.test.ts
  - package.json
autonomous: true
requirements:
  - DIST-05
estimate:
  tokens: 45000
  raw_tokens: 20000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "scripts/audit-release.ts performs comprehensive static and AST analysis verifying: (1) no forbidden CouchDB endpoints/methods are reachable (_compact, _purge, _view_cleanup, database DELETE), (2) zero unredacted secrets exist in logs/state/code, (3) zero out-of-scope storage providers (S3, WebDAV, P2P), and (4) strict dependency pinning with uuid: 11.1.1 overrides."
    - "tests/unit/release-audit.test.ts executes the auditor against source code, package.json, and the bundled release artifact (dist/bundle.cjs)."
    - "The release audit is integrated into package.json via npm run audit:release and passes with 0 violations."
    - "Full end-to-end test suite and packaging gate pass completely, certifying all 52 project requirements."
  artifacts:
    - scripts/audit-release.ts
    - tests/unit/release-audit.test.ts
  key_links:
    - "scripts/audit-release.ts scans src/, dist/, and package.json for safety violations."
    - "tests/unit/release-audit.test.ts automates the static safety verification in vitest."
---

<objective>
Implement the release safety audit suite and execute the final release gate verification across the entire codebase and packaged artifact.

Purpose: Guarantee that the packaged executable contains zero destructive CouchDB maintenance operations, zero unredacted secrets, zero out-of-scope storage dependencies, and strict dependency pinning, satisfying requirement DIST-05.
Output: `scripts/audit-release.ts`, `tests/unit/release-audit.test.ts`, and `npm run audit:release` script in `package.json`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-packaged-mixed-client-release-gate/06-RESEARCH.md
@.planning/phases/06-packaged-mixed-client-release-gate/06-PATTERNS.md
@.planning/phases/06-packaged-mixed-client-release-gate/06-VALIDATION.md
@src/security/transport-guard.ts
@src/security/armed-transport-guard.ts
@src/security/redaction.ts
</context>

<threat_model>
<trust_boundaries>
- Packaged Executable Code -> Remote CouchDB Server (Administrative safety boundary)
- Secret Management -> Diagnostic / Logging Outputs (Confidentiality boundary)
</trust_boundaries>
<threats>
- T-06-07: Accidental Destructive Operation Invocation — Dead code or upstream library helpers inadvertently call database deletion, purge, or compaction on a user's production CouchDB. (Mitigation: Static AST and regex scanner searches the compiled bundle and source code for forbidden endpoints; unit test verifies auditor detects injected violations).
- T-06-08: Credential Leakage in Packaged Artifacts — Hardcoded test credentials or unredacted token patterns packaged into the binary release. (Mitigation: Release auditor scans binary string tables and source files for high-entropy secrets and unmasked authentication headers).
</threats>
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement static release safety auditor</name>
  <files>scripts/audit-release.ts, package.json</files>
  <behavior>
    - `scripts/audit-release.ts` provides functions and CLI runner:
      - `auditPackageJson(pkgPath)`: Verifies exact version pins on direct dependencies and `uuid: 11.1.1` overrides on `pouchdb-core` and `pouchdb-utils`.
      - `auditSourceTree(srcDir)`: Scans TypeScript source for forbidden CouchDB maintenance calls (`_compact`, `_purge`, `_view_cleanup`, `_revs_limit`, `_security`, `DELETE /${db}`), unauthorized cloud providers (AWS S3, WebDAV, Dropbox, Google Drive, WebRTC/P2P), and unredacted logging patterns.
      - `auditBundle(bundlePath)`: Scans bundled CommonJS file (`dist/bundle.cjs`) to confirm no forbidden administrative endpoints or hardcoded test keys exist in the compiled artifact.
      - CLI runner outputs structured report of checked rules and exits 0 on success, or 1 with violation details.
    - Add `"audit:release": "tsx scripts/audit-release.ts"` to `package.json`.
  </behavior>
  <action>
Implement `scripts/audit-release.ts` with comprehensive pattern scanning and rule checkers. Add `audit:release` script to `package.json`.
  </action>
  <verify>
    <automated>npx tsx scripts/audit-release.ts</automated>
    <fails_when>non-zero exit or violation detected</fails_when>
  </verify>
  <done>
`scripts/audit-release.ts` provides complete automated scanning across package dependencies, source trees, and compiled bundles.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement release audit unit test suite</name>
  <files>tests/unit/release-audit.test.ts</files>
  <behavior>
    - Tests `auditPackageJson`:
      - Passes with current `package.json`.
      - Fails if a dependency has a loose semver range (`^`, `~`, `*`).
      - Fails if `uuid: 11.1.1` override is missing.
    - Tests `auditSourceTree`:
      - Passes against current `src/` tree.
      - Fails when mock source file contains forbidden endpoint string or unmasked secret.
    - Tests `auditBundle`:
      - Passes against compiled `dist/bundle.cjs`.
    - Tests runtime guard enforcement: verifies `TransportGuard` and `ArmedTransportGuard` reject destructive and administrative CouchDB URLs.
  </behavior>
  <action>
Implement `tests/unit/release-audit.test.ts`. Verify all audit rules catch both clean state and synthetic violations.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/release-audit.test.ts</automated>
    <fails_when>non-zero exit or test failure</fails_when>
  </verify>
  <done>
Unit test suite proves the release safety auditor accurately detects forbidden operations, loose dependencies, and secret leaks.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Execute complete release gate verification</name>
  <files>package.json, scripts/audit-release.ts</files>
  <behavior>
    - Runs the full test and verification pipeline:
      1. `npm run test:unit`: All unit tests pass.
      2. `npm run build:sea`: Standalone executable builds cleanly.
      3. `npm run audit:release`: Static safety audit passes with 0 violations.
      4. `npx vitest run tests/integration/packaged-binary.test.ts`: Standalone execution in clean environment passes.
      5. `npx vitest run tests/integration/mixed-client-interop.test.ts`: Mixed-client interoperability against CouchDB 3.5.2 passes.
  </behavior>
  <action>
Run the complete release gate verification suite. Ensure all tests and audits pass cleanly and without warnings.
  </action>
  <verify>
    <automated>npm run test:unit && npx tsx scripts/audit-release.ts</automated>
    <fails_when>non-zero exit or test failure</fails_when>
  </verify>
  <done>
All release gate verifications pass cleanly, certifying full compliance with all v1 project requirements (COMP-07, DIST-01, DIST-02, DIST-03, DIST-04, DIST-05).
  </done>
</task>

</tasks>
