---
phase: 02-verified-pull-materialization
plan: 04
type: execute
wave: 4
depends_on:
  - "02-03"
files_modified:
  - src/security/capabilities.ts
  - src/cli/commands/pull.ts
  - src/filesystem/atomic-reflector.ts
  - src/storage/provenance-repo.ts
  - tests/unit/atomic-reflector.test.ts
  - tests/unit/provenance-repo.test.ts
  - tests/unit/transport-guard.test.ts
  - tests/integration/pull-apply.test.ts
autonomous: true
requirements:
  - PULL-02
  - PULL-03
  - PULL-05
estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Created files are staged as a sibling temp, FileHandle.sync is called, rename installs the dest, read-back bytes equal the assembled buffer, then provenance is inserted"
    - "Crash or throw before rename leaves the destination path missing; no file_provenance row is written"
    - "Provenance INSERT happens only after read-back equality; a mismatch throws and writes no row"
    - "Dry-run and empty plans write zero file_provenance rows; observed_mtime may be null"
    - "Commit order is durable bytes, rename, read-back, SQLite INSERT — saveProvenance is never called from buildPullPlan"
    - "Apply issues VaultReflectCapability only after admission plus successful preflight; dry-run never calls createVaultReflectCapability"
    - "A planned create whose destination already exists is not overwritten"
    - "Interrupted apply after rename but before SQLite leaves no provenance row; never the reverse"
    - "A single pull process serializes apply; overlapping processes are not coordinated and must not be assumed to share a write lock"
  artifacts:
    - "VaultReflectCapabilityBrand, createVaultReflectCapability, isVaultReflectCapability in src/security/capabilities.ts"
    - "tests/unit/atomic-reflector.test.ts"
    - "tests/unit/provenance-repo.test.ts"
  key_links:
    - "applyVerifiedPull requires isVaultReflectCapability(cap) and calls installAtomically before ProvenanceRepository.saveProvenance"
    - "ZeroMutationVerifier still wraps apply; CouchDB update_seq is unchanged after a successful materialization"
  prohibitions:
    - "MUST NOT construct the Commonlib file-manipulator coordinator or otherwise issue remote PUT, POST, or DELETE during pull or dry-run (including salt creation)"
    - "MUST NOT silently choose a CouchDB deterministic winner when another live non-deleted leaf exists"
    - "MUST NOT overwrite unproven local vault files or emit decrypted payloads or passphrases in logs, reports, or SQLite"
---

## Phase Goal

**As a** vault operator, **I want to** dry-run and then materialize verified LiveSync remote files into an empty or dedicated vault, **so that** I can recover vault content without granting any remote write capability and without destroying unproven local or remote data.

<objective>
Harden the tracer apply path: capability-gated reflection, crash-safe install proofs, and provenance-after-verify with fail-closed integration coverage.

Purpose: PULL-03 and PULL-05 are only true if install and SQLite ordering are tested, not merely present.
Output: VaultReflectCapability, atomic-reflector and provenance unit tests, expanded pull-apply integration.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-verified-pull-materialization/02-RESEARCH.md
@.planning/phases/02-verified-pull-materialization/02-PATTERNS.md
@src/security/capabilities.ts
@src/cli/commands/pull.ts
@src/filesystem/atomic-reflector.ts
@src/storage/provenance-repo.ts
@src/storage/admission-repo.ts
@tests/unit/admission-repo.test.ts
@tests/unit/transport-guard.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Brand VaultReflectCapability and gate apply</name>
  <files>
    src/security/capabilities.ts
    src/cli/commands/pull.ts
    tests/unit/transport-guard.test.ts
  </files>
  <read_first>
    src/security/capabilities.ts
    src/cli/commands/pull.ts
    tests/unit/transport-guard.test.ts
  </read_first>
  <behavior>
    - createVaultReflectCapability returns an object that passes isVaultReflectCapability and fails isReadCapability
    - runPullCommand dryRun true never calls createVaultReflectCapability
    - applyVerifiedPull throws if the capability argument fails isVaultReflectCapability
  </behavior>
  <action>
    Copy the ReadCapability brand pattern: VaultReflectCapabilityBrand symbol, interface with copied baseUrl, databaseName, vaultRoot, issuedAt, createVaultReflectCapability, isVaultReflectCapability. Issue only after negotiateCompatibility.admitted and preflightVault success. applyVerifiedPull(cap, ...) must check isVaultReflectCapability.

    Extend tests/unit/transport-guard.test.ts capability token cases (lines 174-193 analog) for the new brand.
  </action>
  <acceptance_criteria>
    - isVaultReflectCapability(createReadCapability(...)) is false
    - grep -v '^#' src/cli/commands/pull.ts | grep -c 'createVaultReflectCapability' is at least 1
    - npx vitest run tests/unit/transport-guard.test.ts tests/unit/smoke.test.ts passes
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/transport-guard.test.ts tests/unit/smoke.test.ts</automated>
  </verify>
  <done>
    Apply cannot run with only a ReadCapability; dry-run never receives a reflect capability.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Prove crash-before-rename and provenance-after-readback</name>
  <files>
    src/filesystem/atomic-reflector.ts
    src/storage/provenance-repo.ts
    tests/unit/atomic-reflector.test.ts
    tests/unit/provenance-repo.test.ts
  </files>
  <read_first>
    src/filesystem/atomic-reflector.ts
    src/storage/sqlite.ts
    src/storage/provenance-repo.ts
    tests/unit/admission-repo.test.ts
    .planning/phases/02-verified-pull-materialization/02-RESEARCH.md
  </read_first>
  <behavior>
    - installAtomically writes dest bytes equal to input; a dest that already exists is not truncated in place before rename
    - Simulated failure after temp sync and before rename leaves dest absent
    - Read-back mismatch throws ReadbackMismatchError
    - openDatabase creates file_provenance; PRAGMA table_info column names exclude password, passphrase, secret, authorization, and body
    - saveProvenance then getByPath round-trips remote_revision and content_sha256
  </behavior>
  <reversibility rating="costly">file_provenance column set is the on-disk format Phase 3 resume and later sync will read; changing it needs a new migration.</reversibility>
  <action>
    Add tests/unit/atomic-reflector.test.ts using mkdtemp. Assert sibling temp naming prefix .ols-tmp-. Optionally inject a rename function to simulate pre-rename crash. Confirm dest missing and temp cleaned or left beside dest without replacing dest.

    Provenance tests copy admission-repo.test.ts temp sqlite lifecycle and secret-exclusion PRAGMA table_info assertions (lines 159-177 analog). CURRENT_SCHEMA_VERSION must be 2. The pure planner module must not import the provenance writer.

    If installAtomically still opens the destination with a truncate flag, change it to temp plus rename only.
  </action>
  <acceptance_criteria>
    - tests/unit/atomic-reflector.test.ts asserts dest missing after pre-rename failure
    - tests/unit/provenance-repo.test.ts asserts file_provenance exists and secret-like column names are absent
    - grep -v '^#' src/domain/pull-plan.ts | grep -c 'saveProvenance' equals 0
    - npx vitest run tests/unit/atomic-reflector.test.ts tests/unit/provenance-repo.test.ts tests/unit/admission-repo.test.ts passes
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/atomic-reflector.test.ts tests/unit/provenance-repo.test.ts tests/unit/admission-repo.test.ts</automated>
  </verify>
  <done>
    Install is crash-safe at the rename boundary; provenance stores only revision and hash after verify.
  </done>
</task>

<task type="auto">
  <name>Apply integration: encrypted files, fail-closed blocks, zero mutation</name>
  <files>
    src/cli/commands/pull.ts
    tests/integration/pull-apply.test.ts
  </files>
  <read_first>
    src/cli/commands/pull.ts
    tests/integration/pull-dry-run.test.ts
    tests/integration/inspect-command.test.ts
    tests/integration/pull-apply.test.ts
  </read_first>
  <action>
    Expand tests/integration/pull-apply.test.ts (120s/60s harness): (1) empty vault apply of seeded plain text and newnote binary plus legacy notes — dest bytes match, provenance remote_revision matches each _rev, update_seq unchanged; (2) any block (conflict seed from 02-03 or injected DecodeFailure) results in zero dest files and exit 8 or 7; (3) dry-run false on non-empty non-dedicated vault exits CONFIG_ERROR and does not overwrite; (4) JSON Lines pull_report type pull_report contains no field named bytes or passphrase.

    Confirm applyVerifiedPull order in pull.ts: preflight, createVaultReflectCapability, per-file installAtomically, read-back already inside install, then persist the provenance row, then post snapshot.
  </action>
  <acceptance_criteria>
    - pull-apply covers notes, plain, and newnote materialization
    - Conflict or block apply leaves dest missing
    - npx vitest run tests/integration/pull-apply.test.ts tests/integration/pull-dry-run.test.ts tests/integration/inspect-command.test.ts passes
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/integration/pull-apply.test.ts tests/integration/pull-dry-run.test.ts tests/integration/inspect-command.test.ts</automated>
  </verify>
  <done>
    Operator can materialize a verified vault and can trust that blocked plans write nothing.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vault directory | Local files receive assembled bytes only after validation |
| SQLite state | Provenance is process truth; must not store secrets or plaintext bodies |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Tampering | applyVerifiedPull | high | mitigate | Capability brand plus empty/dedicated preflight plus no overwrite of existing dest |
| T-02-09 | Tampering | ProvenanceRepository | high | mitigate | INSERT only after read-back; dry-run writes zero rows; no secret columns |
| T-02-01 | Tampering | ZeroMutationVerifier around apply | high | mitigate | Apply remains GET/HEAD remotely; post-apply update_seq must match pre |
| T-02-13 | Denial of Service | installAtomically | low | accept | Parent-dir sync may fail on ZFS/Docker bind mounts (RESEARCH A1); file sync plus rename plus read-back remain required |
| T-02-SC | Tampering | npm installs | high | mitigate | No new packages |

Canon breadcrumb: filesystem path-traversal remains $gsd-secure-phase; this plan enforces capability and provenance ordering.
</threat_model>

<verification>
npx vitest run tests/unit/atomic-reflector.test.ts tests/unit/provenance-repo.test.ts tests/unit/transport-guard.test.ts tests/integration/pull-apply.test.ts tests/integration/pull-dry-run.test.ts
</verification>

<success_criteria>
- VaultReflectCapability gates apply
- Crash-before-rename and provenance-after-readback are unit-proven
- Apply integration materializes notes/plain/newnote, fails closed on blocks, and leaves CouchDB unmutated
</success_criteria>

<output>
Create `.planning/phases/02-verified-pull-materialization/02-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- Types: `VaultReflectCapability` (`src/security/capabilities.ts`)
- Functions: `createVaultReflectCapability`, `isVaultReflectCapability`
- Tests: `tests/unit/atomic-reflector.test.ts`, `tests/unit/provenance-repo.test.ts`
- Files: listed in `files_modified`
